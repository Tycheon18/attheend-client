import React from "react";
import styles from './Header.module.css';

const Header = () => {
    return (
        <header className={styles.Header}>
            <h1 className={styles.title}>At the End of the Shelf</h1>
            <nav className={styles.nav}>

            </nav>
        </header>
    )
}

export default Header;